import { SearchSachPipe } from './search-sach.pipe';

describe('SearchSachPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSachPipe();
    expect(pipe).toBeTruthy();
  });
});
