import { SearchNXBPipe } from './search-nxb.pipe';

describe('SearchNXBPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNXBPipe();
    expect(pipe).toBeTruthy();
  });
});
